version: "3.8"
services:
  app:
    build: .
    ports:
      - "8443:8443"
    volumes:
      - ccproxy-data:/app/data
    environment:
      - NODE_ENV=production
      - PORTAL_DATA_DIR=/app/data
    restart: unless-stopped

volumes:
  ccproxy-data:
